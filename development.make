; This file should contain configuration for local development only. For
; instance, you may want to add the devel module in this file and not in
; common.make.

; Inlcude common make file
includes[common] = common.make
